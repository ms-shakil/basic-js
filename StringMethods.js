/*
 thos methods retun true or false .. those are also case sensative
  **startWith  // start string search we can also use index
  **endsWith  // end string chek
  ** includes  // search ani string in main string

*/

// startWith
let message ="Her name is samiya Sultana"
console.log(message.startsWith("Her",0))
console.log(message.endsWith("Sultana"))
console.log(message.includes("samiya"))
